/** Default number of products to fetch from the API. */
export const DEFAULT_FETCH_LIMIT = 100;

/** Number of products per page in the table (client-side pagination). */
export const PAGE_SIZE = 10;

/** Delay (ms) before applying search filter after user stops typing. */
export const SEARCH_DEBOUNCE_MS = 500;

/** How long (ms) the "Product added" success message is shown before hiding. */
export const SUCCESS_MESSAGE_MS = 1500;
